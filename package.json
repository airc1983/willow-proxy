{
  "name": "willow-proxy",
  "version": "1.0.0",
  "description": "Willow — Google Places API proxy for Netlify",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
